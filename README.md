# Make-it-Do-Agent

An agentic host application that dynamically connects to MCP servers, carries out multi-step goals, manages state graphs using LangGraph, and streams execution updates over Server-Sent Events (SSE).
